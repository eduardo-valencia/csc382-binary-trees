{
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "test": "jest --watch",
    "generateDocumentation": "touch README.md && jsdoc2md out/*.js > README.md",
    "build": "tsc --build tsconfig.json"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "dependencies": {
    "jsdoc-to-markdown": "^7.0.1",
    "ts-jest": "^27.0.4"
  }
}
